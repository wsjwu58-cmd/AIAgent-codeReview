name: Run Tests

on:
  push:
    branches: [ master, main, develop, dev, 'feature/**', 'fix/**', 'feat/**' ]
  pull_request:
    branches: [ master, main ]

jobs:
  test:
    name: Build & Test (Java 17)
    runs-on: ubuntu-latest

    env:
      SPRING_PROFILES_ACTIVE: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Run tests (skip AgentEvaluationSuiteTest, needs live Milvus)
        run: mvn test --batch-mode --no-transfer-progress -Dtest='!AgentEvaluationSuiteTest' -DfailIfNoTests=false
